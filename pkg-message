=====================================================================

Note that STAFProc requieres machine's hostname to be resolvable.

If you have not done it yet, please add the following to /etc/hosts:

	::1			<hostname>
	127.0.0.1		<hostname>

To verify do the following:

	ping6 $(hostname)
	ping  $(hostname)

=====================================================================
